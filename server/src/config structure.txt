//СПРАВКА ПО СТРУКТУРЕ CONFIG
//Состоит из 3 уровней: 1 - раздел правил, 2 - сами правила, 3 - вид проверки правила

//-------------1 уровень---------------

    // id: 1, - номер раздела
    // section: "Текст", - название раздела правил (Текст, Заголовки, Кнопки, Картинки, Формы и т.д.). Используется для формирования таблиц с результатами и конструктора правил
    // rules: [{ 2 уровень }] - массив правил каждого раздела


    //---------2 уровень (rules)-----------

        // id: 1, - номер правила
        // selector: "p, div, li, span", - селекторы html разметки, которые проверяются правилом (могут быть представлены тэгами, классами или id)
        // ruleName: "Размер шрифта на ПК от 14px до 24px, целые числа или 330px", - текст правила на русском языке, используется для вывода результата в таблицу
        // сheckers: [{ 3 уровень }] - виды проверки правила (включение, диапазон, кратность, целочисленность). Проверяться может как 1 из 4 видов, так и все 4.


        //--------3 уровень (сheckers)------------

            //----обязательные поля------
            // type: "range" | "integer" | "includes" | "multiplicity", - название типа (диапазон, целочисленный, включение, кратность)
            // toBeParsed: "css-property" | "text-content" | "elements" | "images" , - что будет получаться с элемента (свойство стиля, текстовый контент, сам элемент, изображение)
            

            //----опциональные поля------
            //На данном уровне поля используются опционально, в зависимости от вида проверки (type) и типа получаемого значения (toBeParsed)

            // cssProperty: "fontSize", - используется для типа "css-property" - название css свойства, которое необходимо проверить
            // parseBy: "count" | "byteSize" | "attrs" |..., - дополнительная опция, указывает что нужно узнать у элемента (размер изображения, подсчет количества и другие),

            // range: [14, 24], - используется для типа "range" - диапазон значений
            // variants: [330, 440, ...] | ["normal"] | ["alt"] |..., - используется для типа "includes" - указывает значения, которым должно быть равно значение проверки
            // multiplesOf: [2, 3, 5], - используется для типа "multiplicity" - указывает на числа, которому должно быть кратно свойство элемента
        
        //--------3 уровень (сheckers)------------
    //---------2 уровень (rules)-----------
//----------1 уровень---------------